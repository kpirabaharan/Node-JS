<%- include('../includes/head.ejs') %>
  </head>

  <body>
<%- include('../includes/navigation.ejs') %>
    <main>
      <% if (products.length > 0){ %> 
        <ul>
          <%products.forEach(prodData => {%>
            <li>
              <%=prodData.productData.title%> (<%=prodData.quantity%>)
            </li>
          <%});%>
        </ul>
      <% } else { %>
      <h1>No Prodcuts in Cart!</h1>
      <%} %>
    </main>
<%- include('../includes/end.ejs') %>